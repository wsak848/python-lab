```python
import streamlit as st

# =========================================
# PAGE CONFIG
# =========================================

st.set_page_config(
    page_title="Algorithms Lab",
    page_icon="🧠",
    layout="wide"
)

# =========================================
# TITLE
# =========================================

st.title("🧠 Algorithms Lab")

st.subheader("เรียนรู้การคิดแบบลำดับขั้นตอน")

st.markdown("---")

# =========================================
# THEORY
# =========================================

st.header("📘 Algorithm คืออะไร ?")

st.write("""
Algorithm คือ ลำดับขั้นตอนในการแก้ปัญหา
หรือการทำงานอย่างเป็นขั้นตอน
""")

# =========================================
# EXAMPLE
# =========================================

st.header("☕ ตัวอย่าง Algorithm")

st.write("ขั้นตอนการชงกาแฟ")

steps = [
    "1. เตรียมน้ำ",
    "2. ต้มน้ำ",
    "3. ใส่กาแฟ",
    "4. เทน้ำร้อน",
    "5. พร้อมดื่ม"
]

for step in steps:
    st.success(step)

# =========================================
# INTERACTIVE
# =========================================

st.header("🎮 Interactive Activity")

st.write("เลือกขั้นตอนแรกของการชงกาแฟ")

answer = st.radio(
    "ข้อใดควรทำก่อน ?",
    [
        "เทน้ำร้อน",
        "ต้มน้ำ",
        "เตรียมน้ำ",
        "ดื่มกาแฟ"
    ]
)

if st.button("ตรวจคำตอบ"):

    if answer == "เตรียมน้ำ":
        st.success("✅ ถูกต้อง!")
        st.balloons()

    else:
        st.error("❌ ยังไม่ถูก ลองใหม่")

# =========================================
# MINI CHALLENGE
# =========================================

st.header("🏆 Mini Challenge")

user_algo = st.text_area(
    "อธิบาย Algorithm การมาโรงเรียนของคุณ"
)

if st.button("ส่งคำตอบ"):
    st.success("บันทึกคำตอบแล้ว 👍")
```
